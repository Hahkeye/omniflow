"""Gradio UI (secondary). Prefer CLI + daemon + Tauri for the product path.

Status: maintained for local web demos; feature parity is not guaranteed.
Start with: python -m diary_app.ui  (localhost only by default)
"""
